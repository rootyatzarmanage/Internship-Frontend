import peterAvatar from '../assets/peterparker.jpeg'

// Placeholder "current user" data shown in the sidebar footer.
// Swap this out for real auth/session data later.
export const currentUser = {
    name: 'Peter Parker',
    role: 'Administration',
    email:'peterparker@company.com',
    avatar: peterAvatar,
    logoutRoute: '/logout',
}
